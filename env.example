# ─────────────────────────────────────────────────────────────────────────────
# Thermal Dual Vision — Environment Variables
#
# Copy to .env and adjust.  Most settings (AI key, Telegram, retention, etc.)
# are stored in data/config.json via the web UI — NOT here.
# Only infrastructure / runtime knobs belong in this file.
# ─────────────────────────────────────────────────────────────────────────────

# Log level for the backend (debug | info | warning | error)
LOG_LEVEL=info

# go2rtc HTTP API URL (default points to the embedded go2rtc process)
GO2RTC_URL=http://127.0.0.1:1984

# go2rtc RTSP base URL used by the detector workers to pull camera streams
GO2RTC_RTSP_URL=rtsp://127.0.0.1:8554

# Allowed CORS origins (comma-separated, empty = same-origin only)
# Example: CORS_ORIGINS=http://localhost:5173,http://homeassistant.local
CORS_ORIGINS=

# Max concurrent media encoding jobs (collage + MP4); lower = less CPU spike
MEDIA_MAX_CONCURRENCY=2

# FFmpeg log level passed to subprocesses (error | warning | info | verbose)
FFMPEG_LOGLEVEL=error
